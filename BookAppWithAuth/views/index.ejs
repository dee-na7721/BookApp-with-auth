<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/index.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
      integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <%if(isLoggedIn){%>
    <div style="float: right">
      <a href="/users/logout"><button>Log Out</button></a>
    </div>
    <%}else{%>
    <div style="float: right">
      <a href="/users/login"><button>Log In</button></a>
      <a href="/users/signup"><button>Sign up</button></a>
    </div>
    <%}%>
    <div>
      <h1><%= title %></h1>
    </div>

    <p>
      Users can add the books they read and post details about it. It helps
      others to know about different books.
    </p>
    <%if(isLoggedIn){%>
    <a class="btn btn-success" href="/books/add">Add book</a>
    <%}else{%>
    <button class="btn btn-success" onclick="usermessage()">Add Book</button>
    <br />
    <span id="addbook"></span>
    <%}%>
    <hr />

    <div class="row">
      <% bookList.forEach(function(book, i) { %>
      <div
        class="card"
        style="width: 18rem; margin-left: 30px; margin-top: 30px"
      >
        <div class="card-body">
          <h5 class="card-title"><%= book.title %></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%= book.author %></h6>
          <p class="card-text"><%= book.description %></p>
          <%if(isLoggedIn){%>
          <a href="/books/edit/<%= book._id %>" class="card-link">Edit</a>
          <a href="/books/remove/<%= book._id %>" class="card-link">Delete</a>
          <% } %>
        </div>
      </div>
      <%})%>
    </div>
  </body>
</html>
<script>
  var addbook = document.getElementById("addbook");
  function usermessage() {
    addbook.append("You must be logged in to add books. Please log in first!");
  }
</script>
